meta {
  name: GET list sets
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/sets
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  exerciseId: {{exerciseId}}
  from: 
  to: 
  isBodyweight: 
  limit: 100
  offset: 0
}
